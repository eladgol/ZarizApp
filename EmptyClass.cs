using System;
using Xamarin.Forms;
namespace ZarizNavigation
{
    /// <summary>
    /// This class is for creating the google effect of either the label or the placeholder being showed.
    /// Whwn not in focus the placeholder is shown and the label is hidden, and vice versa when it is in focus
    /// </summary>
    public class MyTextEntry
    {
        public MyTextEntry()
        {
            
        }
    }
}
